{
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ],
    "routes": [
        {
            "src": "/(admin|dashboard)/.*",
            "middlewarePath": "middleware"
        }
    ]
}